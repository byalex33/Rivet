# Empty means every world. Otherwise list exact world names.
allowed-worlds: []

messages:
  enabled: { enabled: true, actions: ['[message] <white>Pose enabled. Move, dismount, or run the command again to leave it.</white>'] }
  disabled: { enabled: true, actions: ['[message] <white>Pose disabled.</white>'] }
  not-allowed: { enabled: true, actions: ['[message] <white>Poses are not allowed in this world.</white>'] }
